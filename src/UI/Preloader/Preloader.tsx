import React from "react";

const Preloader = () => {
  return (
    <div className="flex items-center justify-center w-full h-full">
      <span className="h-3 w-3 rounded-full loader"></span>
    </div>
  );
};

export default Preloader;
